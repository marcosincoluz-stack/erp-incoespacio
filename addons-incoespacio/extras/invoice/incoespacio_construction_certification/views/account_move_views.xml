<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_move_form_inherit_construction" model="ir.ui.view">
        <field name="name">account.move.form.inherit.construction</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">
            <xpath expr="//notebook" position="before">
                <group>
                    <group name="construction_obra" string="Obra / tajo" colspan="2"
                           invisible="move_type not in ('in_invoice', 'in_refund')">
                        <group>
                            <field name="project_id"
                                   string="Proyecto / Obra"
                                   placeholder="Buscar por código o cliente…"
                                   context="{'open_construction_order': 1}"
                                   options="{'no_create': True, 'no_quick_create': True}"/>
                            <field name="retention_order_id" invisible="1"/>
                        </group>
                        <group name="construction_obra_actions"/>
                    </group>
                </group>
            </xpath>
        </field>
    </record>

    <record id="view_invoice_tree_inherit_construction" model="ir.ui.view">
        <field name="name">account.invoice.tree.inherit.construction</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_invoice_tree"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='name']" position="after">
                <field name="construction_order_id" string="Obra" optional="show"/>
            </xpath>
        </field>
    </record>

    <record id="view_account_invoice_filter_inherit_construction" model="ir.ui.view">
        <field name="name">account.move.search.inherit.construction</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_account_invoice_filter"/>
        <field name="arch" type="xml">
            <xpath expr="//search" position="inside">
                <filter string="Obra" name="group_obra" context="{'group_by': 'construction_order_id'}"/>
            </xpath>
        </field>
    </record>
</odoo>
